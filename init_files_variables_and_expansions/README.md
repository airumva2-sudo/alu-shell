# init_files_variables_and_expansions

ALU "Shell, init files, variables and expansions" project. Each script is
exactly two lines: `#!/bin/bash` followed by a single command/expression.
No &&, no ||, no ;, no bc, no sed, no awk.
