# io_redirections_and_filters

ALU "Shell, I/O Redirections and filters" project. Each script is exactly
two lines: `#!/bin/bash` followed by a single command/pipeline.
No backticks, no &&, no ||, no ;, no sed, no awk.

See each file for what it does (described at the top of every script via
the task title).
